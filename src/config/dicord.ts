const scopes = {
    tryNow: "bot identify email guilds",
    login: "identify",
    connectGuild: "bot",
};

const permissions = {
    ViewChannels: 0x400,
    manageServer: 0x20
}

export {
    scopes,
    permissions
}

