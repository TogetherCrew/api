const scopes = {
    login: "identify email guilds",
    bot: "bot applications.commands identify email guilds"
};

const permissions = {
    ViewChannels: "1024"
}

export {
    scopes,
    permissions
}

