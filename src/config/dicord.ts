const scopes = {
    tryNow: "bot applications.commands identify email guilds",
    login: "identify",
    connectGuild: "bot applications.commands",
};

const permissions = {
    ViewChannels: 0x400,
    manageServer: 0x20
}

export {
    scopes,
    permissions
}

